import React, { Component } from "react";
import {render} from "react-dom";

class Room extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return (
        <h2> Room </h2>
    );
  }
}

export default Room;
